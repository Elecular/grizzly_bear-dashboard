import Experiments from "dashboardViews/Experiments";

const routes = [
    {
        path: "/experiments",
        name: "Experiments",
        icon: "tim-icons icon-atom",
        component: Experiments,
        layout: "/dashboard",
    },
];

export default routes;
